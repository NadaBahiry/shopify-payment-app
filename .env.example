# Shopify App Configuration (required for dev and production)
SHOPIFY_API_KEY=your_shopify_api_key
SHOPIFY_API_SECRET=your_shopify_api_secret
SHOPIFY_APP_URL=https://your-app-url.example.com
SCOPES=read_orders,write_orders,write_products

# Production: use a PostgreSQL (or other) database URL.
# Omit for local dev (Prisma defaults to prisma/dev.sqlite).
# DATABASE_URL=postgresql://user:password@host:5432/dbname
